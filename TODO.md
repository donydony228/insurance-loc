# TODO

承 `FINDINGS_新增通訊處相關性.md` 的兩個下一步。

## 1. 改變 X 的問法:計數 → 是/否

**問題:** 新增通訊處數在鄉鎮市區層級有 92% 是 0(1,840 格裡只有 139 格非零)。皮爾森係數是為連續變數設計的,套在這種零膨脹計數上,算出來的 r 被那一大票 0 主導。

**做法:** 改問「**這個區這 5 年有沒有新設通訊處**」(是/否),比較兩組的差異。二分之後的皮爾森就是點二系列相關,統計上正當,而且結論直接可讀:

```
有新增的區:人口密度中位數 5,819 人/km²
沒有的區:                 544 人/km²   → 差 10.7 倍
```

比「r = 0.379」好懂得多,也好對主管解釋。

### 分析單位要先改(比選檢定更重要)

不能用「區 × 年」的 1,840 列分兩組 —— 同一個區重複 5 次,兩組內部不獨立(pseudo-replication),p 值會嚴重高估。單位要改成**鄉鎮市區本身**:

```
n = 368,有開過(5 年內至少 1 家)79 個,沒開過 289 個
```

### 用哪種檢定:Mann-Whitney U

人口密度偏態 +3.11(超過 2 即嚴重違反常態),t 檢定前提不成立。U 檢定用名次,不假設分布。

注意:**點二系列相關 = 獨立樣本 t 檢定**,數學上等價。所以「把 X 二分後丟進現有的 `pearson()`」其實就是在做 t 檢定,會繼承常態假設,對這份資料不合適。若主管一定要看 t 檢定,**先取對數**(密度取對數後偏態降到 −0.21,t 值從 6.56 變 12.52 —— 原始值反而被離群值稀釋)。

### 要報三個數字,不能只報 p

n=368 時幾乎什麼都顯著,p 只回答「有沒有差」,不回答「差多少」。必須配效果量:

- **AUC(優勢機率)** —— 最好解釋:AUC=0.85 代表「隨機抽一個有開的區和一個沒開的區,有開那個密度較高的機率是 85%」
- **Cliff's δ**(= 2×AUC − 1),門檻 0.147 小 / 0.33 中 / 0.474 大

### 已跑出來的結果

| Y | 有開(中位) | 沒開(中位) | 倍數 | p | AUC | δ | 效果量 |
|---|---|---|---|---|---|---|---|
| 年底人口 | 123,004 | 22,202 | 5.54 | <0.001 | 0.88 | +0.768 | 大(但是規模效應) |
| **人口密度** | 3,981 | 440 | 9.05 | <0.001 | 0.85 | +0.710 | 大 |
| 綜合所得平均數 | 771 | 628 | 1.23 | <0.001 | 0.81 | +0.610 | 大 |
| **粗死亡率** | 8.4 | 12.1 | 0.69 | <0.001 | 0.23 | −0.546 | 大 |
| 綜合所得中位數 | 503 | 435 | 1.16 | <0.001 | 0.77 | +0.542 | 大 |
| 老化指數 | 145.5 | 219.9 | 0.66 | <0.001 | 0.29 | −0.414 | 中 |
| 40–64 歲占比 | 38.6 | 39.4 | 0.98 | <0.001 | 0.37 | −0.250 | 小 |
| 65 歲以上占比 | 18.6 | 20.4 | 0.91 | 0.002 | 0.38 | −0.232 | 小 |
| 扶養比 | 43.9 | 42.8 | 1.03 | 0.068 | 0.57 | +0.134 | 可忽略 |
| 粗出生率 | 5.7 | 5.4 | 1.07 | 0.502 | 0.52 | +0.049 | 可忽略 |

**換檢定後兩個結論變了:**

1. **粗死亡率被 Pearson 低估了** —— r=−0.259 看起來弱,U 檢定下 δ=−0.546 是「大」效果量。Pearson 被偏態拖累,U 檢定不受影響。
2. **年底人口 δ=+0.768 排第一,但那是規模效應** —— 換檢定不會自動排除規模,判斷標準還是原來那套。實質結論仍是人口密度。

**多重比較:** 測 11 個 Y,α=0.05 下期望約 0.55 個假陽性。要嚴謹用 Bonferroni,門檻 0.05÷11=0.0045 —— 65 歲以上占比(p=0.002)剛好還過。

### 要動的地方

1. `scripts/` 加一支跑 U 檢定的腳本,輸出上表(目前是臨時算的,沒有落地成程式)。
2. 試算頁面加「數值 / 有無(二分)」模式;選二分時單位自動改成地區(不是地區×年),結果區改顯示兩組中位數、倍數、AUC、δ、U 檢定 p,而不是 r/ρ/r²。

## 2. 人口密度 × 所得 兩軸交叉,產出白地名單

**理由:** 這兩個是唯二通過四項檢查(ρ 落差小、去離群後不掉、逐年同號、率型非規模)的指標,而且量的是不同面向 —— 密度是都會化程度,所得是消費力。

**做法:** 篩「**高密度 + 高所得 + 這 5 年零新增**」的鄉鎮市區,就是白地名單。

**接口:** 這正好接上 `TODO_whitespace_expansion.md` 軌道 A 已完成的白地綜合評分 —— 那邊用的是**現況快照**的加權分位數,這邊多了「5 年內同業有沒有進場」這個**動態**維度。兩者合併看:評分高、但同業近 5 年也沒動的,才是真的還沒被發現;評分高且同業已經在開的,是已經被看到的市場。

---

## 完成後回填

兩項做完後把結論補進 `FINDINGS_新增通訊處相關性.md`。
