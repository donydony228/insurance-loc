# 相關性試算介面 — 實作盤點

目標:一個介面,選兩個特徵 → 算出皮爾森相關係數(例:「該縣市當年新增通訊處數」× 「該縣市當年出生率」)。

---

## 0. 資料形狀(先講清楚,後面都跟著這個走)

一列 = **一個縣市的某一年**。欄位就是各個指標。也就是一張標準的寬表:

| 年 | 縣市 | 新增通訊處數 | 累計通訊處數 | 年底人口 | 出生數 | 粗出生率 | 死亡數 | … |
|---|---|---|---|---|---|---|---|---|
| 109 | 臺北市 | 3 | 214 | 2602418 | 15000 | 5.76 | … | |
| 109 | 新北市 | 5 | 301 | 4030954 | 26000 | 6.45 | … | |
| … | | | | | | | | |
| 113 | 連江縣 | 0 | 0 | 14000 | 90 | 6.43 | … | |

試算就是:選兩欄 → 對所有列算皮爾森。

**樣本數 n = 縣市數 × 年數 = 22 × 5 = 110。**

這一點很關鍵。如果是「固定一個縣市、看它歷年」那 n=5,皮爾森完全沒有統計意義(要 |r|>0.878 才 p<0.05)。改成 pooled panel 之後 n=110,|r|>0.19 就達 p<0.05,是能用的樣本量。

順帶一提,這個形狀還讓另外兩種分析變成「只是加一個篩選」,不用另外設計:
- 只留某一年 → 橫斷面(n=22,比較縣市之間)
- 只留某個縣市 → 時間序列(n=5,太少,僅供看趨勢)
- 不篩 → pooled(n=110,預設)

**若嫌 n 不夠,把層級從縣市換成鄉鎮市區,同一張表變成 368 × 5 = 1840 列。** 資料來源全部都有鄉鎮市區粒度(見下),建表腳本多一個 `--level` 參數就好。缺點是通訊處新增數在鄉鎮市區層級會極度零膨脹(大多數格子是 0)。**建議兩個層級都產出,預設用縣市。**

---

## 1. 資料盤點:這張表的每一欄從哪來

### 已經有的

| 欄位 | 來源檔 | 粒度 | 年份範圍 |
|---|---|---|---|
| 新增通訊處數、累計通訊處數 | `data/通訊處/鄉鎮市區_通訊處_歷年設立數.csv` | 鄉鎮市區 × 公司 × 年 | 1962–2026 |
| 平均每戶消費支出 | `data/income/縣市_平均消費支出.csv`(寬表,要 melt) | **縣市** | 1998–2024(民國 87–113) |

### 要補的(已實測可抓,都是 `step1`–`step4` 用過的同一批來源,不用另找)

| 欄位 | 來源 | 粒度 | 實測年份範圍 |
|---|---|---|---|
| 年底人口、人口密度、土地面積 | 戶政司 `ODRP048/{yyy}` | 鄉鎮市區(370 筆,1 頁) | **109–114** |
| 出生數 → 粗出生率 | 戶政司 `ODRP055/{yyy}` | 鄉鎮市區(約 27,000 筆,14 頁) | **109–113** |
| 死亡數 → 粗死亡率、自然增加率 | 戶政司 `ODRP031/{yyy}` | 鄉鎮市區(368 筆,1 頁) | **109–113** |
| 戶數 → 平均戶量 | 戶政司 `ODRP019/{yyy}` | 村里(約 7,760 筆,4 頁) | **109–113** |
| 年齡結構(40–64 占比、65+ 占比) | 戶政司 `ODRP021` 或 `ODRP052`(含同婚版) | 村里 | 109–113 |
| 綜合所得中位數 / 平均數 | 財政部 `fia.gov.tw/WEB/fia/ias/ias{yyy}/{yyy}_165-9.csv` | 鄉鎮市區 | **109–112**(113 尚未發布,404) |
| (可選)結婚/離婚對數 | 戶政司 `ODRP068`/`ODRP070` | 鄉鎮市區(618,240 筆,310 頁,較重) | 109–113 |

### 年份窗口的硬限制

實測過 `ODRP019 / 031 / 048 / 055 / 068 / 116`,**民國 105 以前一律「查無資料」**。戶政司 open data 只保留約 5 年滾動窗口,這是來源端的限制,不是抓法問題。

所以:

- **核心欄位(人口/出生/死亡/通訊處)= 109–113,5 年 → n=110**
- 加上所得欄位 = 109–112,4 年 → n=88
- 消費支出可回溯到民國 87,但只有它一欄長,對交集沒幫助

**→ 建表時要「逐欄取交集」,而不是整張表砍到最短的那一欄。** 介面上選了哪兩欄,就用那兩欄的年份交集,並顯示實際 n。

若五年不能接受,唯一的路是改用戶政司統計年報的 PDF/XLS 人工整理(成本高),範圍要另議。

### 兩個會讓係數失真的資料陷阱

1. **通訊處資料有存活者偏誤。** `設立日期` 來自各公司官網**現存**通訊處,已裁撤的不在資料裡。所以「109 年累計總數」的真正意思是「今天還活著的通訊處中,109 年前設立的有幾家」,不是「109 年當時有幾家」。年代越久遠低估越嚴重。
   → **累計通訊處數** 這一欄拿去算歷年相關會系統性偏誤;**新增通訊處數** 影響小很多(只缺當年設立、後來收掉的)。109–113 這個窗口很近,影響有限,但欄位說明要標註。
2. **新增通訊處數是零膨脹計數。** 實算過:21 縣市 × 5 年 = 105 格,其中 **43 格是 0**(41%)。皮爾森假設連續、近似常態,對這種資料容易被少數大值主導。
   → 介面同時給 Spearman 當對照(多寫 3 行,見步驟 5)。兩個係數差很多就是警訊。

另外:**連江縣 6 家壽險一家通訊處都沒有**,通訊處資料只涵蓋 21 個縣市。建表時連江縣要保留、新增數填 0 —— 那是真實觀測值(離島沒設點),不是缺值,不能 drop。

---

## 2. 實作步驟

### Step 1 — 建面板表(核心工作,沒有它介面沒東西可算)

新增 `scripts/build_panel.py`,產出兩份:

```
data/panel/縣市年度面板.csv       22 縣市 × 年   ← 預設
data/panel/鄉鎮市區年度面板.csv    368 區 × 年     ← 需要更大 n 時用
```

欄位:`年, 縣市[, 鄉鎮市區], <各指標>`,一列一個 (年, 地區)。

實作要點:

- **各來源分開抓、分開存 raw,最後再 join** —— 沿用 `step1`–`step4` 的既有寫法:每支 API 一個 `fetch_*()`,抓下來先落地 raw CSV,再合併。這樣某一支掛了不用整個重跑。
- **分頁與 `site_id` 拆解邏輯 `step3_growth_migration.py` 已經寫過**(它已在用 ODRP048),直接複用,不要重寫一份。
- **`site_id` 格式是 `"新北市板橋區"`(縣市+區連在一起)**,要拆。縣市/鄉鎮市區用字統一用「臺」不用「台」,對齊 `step0_geocode.py` 已建立的慣例,否則 join 不上。
- **縣市層級的彙總方式要分兩類**,不能一律 sum:
  - 計數型(通訊處數、出生數、死亡數、人口數、戶數)→ 直接加總
  - 比率型(出生率、密度、平均戶量、所得中位數)→ **必須用縣市層級的分子分母重算**,不能把鄉鎮市區的率平均起來。中位數更是完全不能平均,要用財政部原始檔裡的縣市合計列(`step4_income.py` 已經處理過同樣的問題,那邊是取「村里=合計」列,這裡取縣市合計列)。
- **通訊處的 rollup**:`鄉鎮市區_通訊處_歷年設立數.csv` 裡 `公司='全部'` 的列加總即可(注意不要把各公司列一起加,會變兩倍)。
- **缺值留空,不要填 0** —— 除了連江縣通訊處數那種真實的 0。皮爾森那端靠 `Number.isFinite` 成對排除。

**驗證**:全國加總對得起來(如出生數合計 vs 戶政司公布的全國數)、22 縣市 × 年數 = 預期列數、通訊處欄位加總 = 該年份區間的實際筆數。

### Step 2 — 介面放哪

建議直接長在 `data/通訊處/dashboard.html`,理由是這幾樣現成可複用:

- `buildGroupedOptions()` — 分組下拉選單建構工具(圖層選單和篩選條件已共用)
- `dashboard_inject_district_layer.py` 的 marker 注入模式 — 加一組新 marker(`CORRELATION_HTML_START/END`、`CORRELATION_JS_START/END`)就能安全重跑整段替換,**不開新腳本、不開新檔案**
- 既有的側欄樣式與 `escapeHtml` 等工具

面板 CSV 很小(110 列),轉成 JSON 直接內嵌進 HTML 即可,不需要 fetch(dashboard.html 本來就是單檔內嵌資料的設計)。

次選:獨立一頁 `correlation.html`。但那樣共用工具要抽出來,多一層維護成本,除非你希望試算跟地圖完全分開。

### Step 3 — UI

- X 欄下拉、Y 欄下拉(選項 = 面板表的指標欄位,用 `buildGroupedOptions` 分組:通訊處 / 人口 / 生育死亡 / 所得消費)
- 篩選:年份(全部 / 單一年)、縣市(全部 / 單一縣市)、層級(縣市 / 鄉鎮市區)
- 結果區:r、n、實際使用的年份範圍、散佈圖

### Step 4 — 皮爾森

標準庫夠用,不加任何依賴:

```js
function pearson(pairs) {
  const n = pairs.length;
  const mx = pairs.reduce((s, p) => s + p[0], 0) / n;
  const my = pairs.reduce((s, p) => s + p[1], 0) / n;
  let sxy = 0, sxx = 0, syy = 0;
  for (const [x, y] of pairs) {
    const dx = x - mx, dy = y - my;
    sxy += dx * dy; sxx += dx * dx; syy += dy * dy;
  }
  return sxy / Math.sqrt(sxx * syy);
}
```

配對時用 `Number.isFinite` 成對排除缺值,並把實際 n 顯示出來。

### Step 5 — Spearman 當對照

把每欄換成排名後餵進同一個 `pearson()`,不用另寫公式。零膨脹的通訊處數、重尾的所得,兩者一定要對照著看。

### Step 6 — 散佈圖

inline SVG 手畫(約 20 行:兩軸 + 110 個 `<circle>` + 迴歸線)。**不要**為了一張散佈圖加 Chart.js / Plotly / D3 —— 頁面已經載了 Leaflet + markercluster,再加一個圖表庫只為畫點不划算。顏色和軸走 `dataviz` skill,跟現有色階同一套系統。

散佈圖比係數本身更重要:能一眼看出是不是台北市/新北市這幾個大點在主導整條線。建議點的 tooltip 顯示「113 臺北市」,離群值直接認得出是誰。

### Step 7 — 三個必須顯示的警語

pooled panel 有它特有的坑,這些不是加分項,是沒有就會給出錯誤結論:

1. **同一縣市重複出現 5 次,不是 110 個獨立樣本。** 顯著性會被高估(信賴區間過窄)。r 本身還是有參考價值,但不要拿 p 值當結論。
2. **共同時間趨勢造成的假相關。** 出生率逐年下降、通訊處逐年累積,兩條各自有趨勢的線放一起,係數自然接近 ±1,但那只是「都跟年份有關」。
   → 提供「改用逐年變化量(一階差分)再算」的 checkbox,幾行 `arr.map((v,i) => v - arr[i-1])` 就能做。
3. **規模效應。** 新北市什麼都大(人口大、通訊處多、出生數多),不做標準化的話幾乎任兩個計數型欄位都會呈現高相關,那是規模不是關係。
   → 計數型欄位(出生數、通訊處數)預設提示改用率型欄位(粗出生率、每萬人通訊處數);率型欄位在 Step 1 就先算好放進表裡。

### Step 8 — 驗證

照 `TODO_population_data.md` 既有做法,用 Playwright 開無頭瀏覽器,選幾組欄位組合,跟 Python `scipy.stats.pearsonr` 獨立算的結果比對到小數第 4 位;另外驗年份交集(選一個 109–113 的欄位 × 一個 109–112 的欄位,n 要是 88 不是 110)。

---

## 3. 建議順序

1. **Step 1 建面板表** —— 這是全部的重點,做完就算不做介面,用 pandas `.corr()` 也能立刻試算
2. Step 2–4(內嵌 + 兩個下拉 + 皮爾森)→ 可用的最小版本
3. Step 6 散佈圖 → 讓結果可判讀
4. Step 5、7、8(Spearman + 警語 + 驗證)

---

## 4. 要先決定的兩件事

- **五年夠不夠?** 戶政司 open data 只有 109–113,n=110(縣市)或 1840(鄉鎮市區)。要更長只能人工整理統計年報,那是另一個規模的工作。
- **層級預設縣市還是鄉鎮市區?** 縣市 n=110、每格數字穩定;鄉鎮市區 n=1840、但通訊處新增數大多是 0。建議兩份都產、預設縣市,介面上可切。
